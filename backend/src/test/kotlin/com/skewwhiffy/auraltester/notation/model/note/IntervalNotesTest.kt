package com.skewwhiffy.auraltester.notation.model.note
/*

import lombok.`val`
internal class IntervalNotesTest {
    @org.junit.jupiter.api.Test
    fun instantiatesCorrectly() {
        val bottomNote: `val` = com.skewwhiffy.auraltester.test.util.TestData.random().absoluteNote()
        val interval: `val` = com.skewwhiffy.auraltester.test.util.TestData.random().interval()
        val expectedNotes: `val` = java.util.List.of(bottomNote, bottomNote.plus(interval))
        val actual: `val` = IntervalNotes(bottomNote, interval)
        assertThat(actual.getNotes()).isEqualTo(expectedNotes)
    }
}


 */