export const fromClef = () => {
}

/*
fun getAbc(clef: Clef): AbcProvider {
    val displayName = getTitleCase(
        clef.displayName,
        "Clef Notes"
    )
    return SingleLineAbc(
        displayName,
        clef,
        NoteLength.semibreve,
        clef.notes.map { it.notes }
)
}
 */